CREATE TABLE MEMBERS (
MEMBER_ID INTEGER PRIMARY KEY AUTOINCREMENT,
USERNAME VARCHAR(50)UNIQUE NOT NULL, 
PASSWORD VARCHAR(100) NOT NULL,
F_NAME VARCHAR(50) NOT NULL,
L_NAME VARCHAR(50) NOT NULL,
PHONE_NUM VARCHAR(20) NOT NULL,
EMAIL VARCHAR(100) NOT NULL,
CARD_NUMBER VARCHAR(20) NOT NULL
);

CREATE TABLE FINANCIAL_TRANSACTIONS (
FINANCIAL_ID INTEGER PRIMARY KEY AUTOINCREMENT,
MEMBER_ID INTEGER NOT NULL,
CUR_DATE DATE NOT NULL, 
AMOUNT VARACHAR(20) NOT NULL,
FOREIGN KEY(MEMBER_ID) REFERENCES MEMBERS(MEMBER_ID)
);

CREATE TABLE RESERVATIONS (
RESERVATION_ID INTEGER PRIMARY KEY AUTOINCREMENT,
MEMBER_ID INTEGER NOT NULL,
CUR_DATE DATE NOT NULL,
TRANS_TYPE TEXT CHECK (TRANS_TYPE IN('BOOK', 'EDIT', 'CANCEL')) NOT NULL,
FOREIGN KEY(MEMBER_ID) REFERENCES MEMBERS(MEMBER_ID)
);

CREATE TABLE ADMIN (
ADMIN_ID INTEGER PRIMARY KEY AUTOINCREMENT,
USERNAME VARCHAR(50) NOT NULL,
EMAIL VARCHAR(100) NOT NULL,
PASSWORD VARCHAR(100) NOT NULL,
F_NAME VARCHAR(50) NOT NULL,
L_NAME VARCHAR(50) NOT NULL,
PHONE_NUM VARCHAR(20) NOT NULL
);

INSERT INTO MEMBERS (USERNAME, PASSWORD, F_NAME, L_NAME, PHONE_NUM, EMAIL, CARD_NUMBER)
VALUES ('grantj03', 'password', 'Grant', 'Jones', '4044503628', 'grantwjones@outlook.com', 1234567890123456 );


INSERT INTO FINANCIAL_TRANSACTIONS (MEMBER_ID, CUR_DATE, AMOUNT)
VALUES (1,'4/4/2024' ,'$19.99');

INSERT INTO RESERVATIONS (MEMBER_ID, CUR_DATE, TRANS_TYPE)
VALUES (1,'4/4/2024', 'BOOK');

INSERT INTO ADMIN (USERNAME, EMAIL, PASSWORD, F_NAME, L_NAME, PHONE_NUM)
VALUES ('bradyb', 'bradyb@gmail.com', 'internetprogram', 'Brady', 'Blackstone', '1111111111');